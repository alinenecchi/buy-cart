import React from "react";

import { Container } from "../styles/components/Wrapper.js";

const components = ({ children }) => {
  return <Container>{children}</Container>;
};

export default components;
